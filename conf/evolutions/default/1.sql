# --- !Ups

create table "clowder" (
  "id" bigint generated by default as identity(start with 1) not null primary key,
  "name" varchar not null,
  "color" varchar not null,
  "breed" int not null,
  "gender" int not null,
  "filename" varchar not null
);

# --- !Downs

drop table "clowder" if exists;
